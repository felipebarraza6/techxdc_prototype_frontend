import { Typography } from 'antd';

const { Title } = Typography;

export const Home = () => {
  return (
    <div>
      <Title>Welcome to the Application</Title>
    </div>
  );
};

export default Home;
